<button class="{{color}} {{ [
  variant == 'default' ? 'text-white bg-'~color: '',
  variant == 'outline' ? 'text-black border-'~color : '',
  'font-bold py-2 px-4 border-b-4 hover:border-b-2 hover:border-t-2 rounded',
] | sort | join(' ') | trim }}">
{{button_content}}
</button>
